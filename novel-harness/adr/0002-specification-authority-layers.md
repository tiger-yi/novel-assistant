---
status: accepted
---

# 分层管理规范权威

Novel Assistant 将 `AGENTS.md` 限定为人工入口和跨项目原则，由 `context.manifest.yaml` 管理机器路由、加载顺序与验证命令，由命令协议和专项规范定义行为语义与 INV，README 只说明用户操作。该分层牺牲单文件信息完整度，换取规则单一归属和可验证引用，避免流程、阈值与领域规则在多份入口文档中漂移。
