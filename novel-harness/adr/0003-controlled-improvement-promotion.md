---
status: accepted
---

# 采用受控改进与人工晋升

Novel Assistant 允许 Agent 从重复失败、人工纠正和事务恢复中形成 `IMP-*` 改进提案，但提案不具备规则效力，也不能自动修改 AGENTS、规范、ADR 或 INV。提案必须经过证据验证与用户批准，才能晋升为普通规范、INV 或 ADR；这一设计牺牲自动规则演化速度，换取规范稳定性、可审计性和对模型误判的隔离。
