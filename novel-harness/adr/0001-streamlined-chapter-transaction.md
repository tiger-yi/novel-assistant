---
status: accepted
---

# 统一章节事务并采用条件归档

Novel Assistant 将正常章节入口统一为 `创作第 N 章`：系统自动采用通过逻辑审计的推荐分支，以准备/提交两阶段保证正文与 World Bible 一致，并在到期且迁移范围无歧义时自动归档。保留 `构思第 N 章`、`更新世界` 和 `归档世界` 作为只读预演及恢复维护入口；这一设计用事务日志、幂等键和歧义熔断换取更少的人工步骤，避免把高风险冲突静默自动化。
